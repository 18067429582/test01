7，测试连接，数据库是否连接成功，springboot主类是否可以启动成功
8，访问项目的静态资源是否可以正常加载。所有的静态资源复制到static目录中
   注意：idea对于就是代码的兼容性较差，编写的js代码有时候无法加载
       解决方式：1，清理idea缓存
                2，clear--instal
                3，rebuild
                4,重启idea和操作系统

9，用户注册
    1，创建t_user表
    2，创建用户实体类,
        通过表的结构可以将表中的公共字段提取出来，放在一个实体类中：BaseDomain,其他表中有公共字段的只需要继承
        这个BaseDomain就行
    3,注册-持久层
       *规划需要执行的sql语句
       *通过mybatis操作数据库
       *设计接口和抽象方法
       *定义映射文件，与对应的接口进行关联。
       *在启动类配置接口文件的位置
       *mapper.xml文件属于资源文件，统一放在resources目录下的mapper文件夹中
          配置：mybatis.mapper-locations=classpath:mapper/*.xml
    4，注册-业务层
       *规划异常
         -密码错误，用户名被占用.....抛出异常
    5，注册-控制层
    6，注册-前端页面